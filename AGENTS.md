# Learn DSH 仓库说明

## 目标

这是非官方中文学习项目。优先保证每章能理解、能运行、能观察、能自动验收，再扩充章节数量。

## 内容规则

- 中文优先；技术术语、命令、标识符和 URL 保留原文。
- 一章只增加一个核心机制。
- 每章必须同时有正向实验、失败或边界实验、预期结果和自动测试。
- fake、mock、教学实现和真实上游实现必须明确区分。
- 上游源码引用固定到 `UPSTREAM.md` 中的 commit，不引用浮动行号。
- 不复制上游整文件、`vendor/`、构建产物、品牌素材或任何密钥与会话数据。
- 不添加 Codex hooks。

## 验证

修改代码后运行：

```bash
pnpm check:course
```

修改章节说明后，还要从 clean clone 视角逐步复跑 README，并在 `validation/` 记录命令、环境、结果和结论。
