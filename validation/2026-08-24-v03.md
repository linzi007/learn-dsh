# v0.3 验证：Tool → AgentLoop → Permission

## 范围

- S05：真实 `ToolRuntime + defineTool` 的 schema、参数、canonical value、renderer 与 Plugin dispose。
- S06：真实 `AgentLoop` 的两步工具闭环；唯一模型替身是无网络 `ScriptedLlmAdapter`。
- S07：真实 `tools/pre-execute + ApprovalService` 的 policy matrix、一次性授权、审计与 fail-closed。

三个章节均不需要 API Key。S07 的内存写入只用于观察 dispatch，不代表 filesystem 或 process sandbox enforcement。

## 环境

- 日期：2026-08-24
- OS：macOS（Darwin arm64）
- Node.js：`v24.19.0`
- pnpm：`11.7.0`
- 上游：`0.1.1-rc.2` / `b150a551b8d465e31e418e1b2eaf5e79bbb7d28e`

## 验证命令

```bash
corepack pnpm exec vitest run s05_tool_contract/tests s06_keyless_agent_loop/tests s07_permission/tests
corepack pnpm demo:s05
corepack pnpm demo:s06
corepack pnpm demo:s07
corepack pnpm lint
corepack pnpm typecheck
corepack pnpm test
git diff --check
```

## 当前结果

- S05-S07：6 个测试文件、38 个测试通过。
- S05 demo：4/4 PASS。
- S06 demo：2/2 PASS；正向为 2 个 Step，负向在第三个拟议 Step 前按课程预算阻断。
- S07 demo：3/3 PASS；第二次 publish 重新询问，无 answerer 与 `policy: never` 均 fail closed。
- 各章独立 lint 与 strict TypeScript 检查通过。
- v0.5 收口时再次执行全仓检查：lint 0 warnings、typecheck 0 errors、27 个测试文件 / 155 个测试通过，S01-S13 的 `demo:all` 全部通过。
- 不包含 `.git`、`node_modules`、IDE metadata 与缓存的 clean-copy 从 frozen install 开始复验，`check:course` 与 `demo:all` 均通过。
- `pnpm audit --prod --registry=https://registry.npmjs.org` 未发现已知漏洞；生产依赖许可证扫描完成。
- S05-S07 已做过章节级故障注入并恢复；负向行为也由自动测试持续覆盖。

## 结论

- 课程工件：`Pass locally`。
- 远程 GitHub Actions：`Not yet`；v0.5 尚未 commit / push。
- 学习者掌握：`Not yet`；课程建成不等于学习者已经亲手完成实验。
