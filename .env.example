# DeepSeek 官方适配器默认从这个变量读取凭据；只在本机 .env 填值。
DEEPSEEK_API_KEY=

# Learn DSH 自定义的模型选择变量，不是 DeepSeek Harness 上游变量。
DEEPSEEK_MODEL=deepseek-v4-flash

# 仅在你明确信任自定义网关时取消注释；它会收到 Key、Prompt 与会话标识。
# DEEPSEEK_BASE_URL=https://your-trusted-gateway.example

# 把 Harness 生成的匿名本机标识限制在当前仓库的忽略目录。
DSH_HOME=.dsh
